- name: install multiple  packages
  hosts: web
  become: yes
  tasks:
  - name: install multiple packages
    ansible.builtin.package:
     name: "{{ item }}"
     state: installed
     
    loop:
    - gcc
    - nginx
    - mysql